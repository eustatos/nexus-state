# Отладка и настройка демонстрации Nexus State

## 🐛 Отладка проблем

### 1. DevTools не работают

**Симптомы:**
- В Redux DevTools не отображаются действия
- Консоль показывает ошибки подключения
- Состояние не отслеживается

**Решение:**
1. Убедитесь, что расширение Redux DevTools установлено
2. Проверьте, включены ли DevTools в настройках приложения (чекбокс в шапке)
3. Откройте консоль разработчика и проверьте наличие ошибок
4. Перезагрузите страницу

### 2. Вычисляемые атомы не обновляются

**Симптомы:**
- Изменения в базовых атомах не отражаются в вычисляемых
- Компоненты показывают устаревшие значения
- Счетчики ререндеров не увеличиваются

**Решение:**
1. Проверьте зависимости вычисляемых атомов
2. Убедитесь, что базовые атомы действительно изменяются
3. Проверьте консоль на наличие ошибок в функциях вычисления
4. Используйте React DevTools для проверки пропсов компонентов

### 3. E2E тесты падают

**Симптомы:**
- Тесты не находят элементы на странице
- Таймауты при ожидании элементов
- Ошибки подключения к серверу

**Решение:**
1. Убедитесь, что сервер запущен на порту 5173
2. Проверьте, что приложение загружается без ошибок
3. Увеличьте таймауты в тестах если необходимо
4. Запустите сервер вручную и проверьте доступность

## 🔧 Настройка окружения

### Требования
- Node.js 16+
- npm 7+
- Браузер с поддержкой ES6 modules

### Установка
```bash
# Клонирование репозитория
git clone <repository-url>
cd nexus-state

# Установка зависимостей
npm install

# Запуск демо
cd apps/demo-react
npm install
npm run dev
```

## 🧪 Запуск тестов

### Unit тесты
```bash
# Все unit тесты
npm run test:unit

# Конкретный тест
npm test -- tests/computed-atoms.test.js

# С покрытием кода
npm run test:coverage
```

### E2E тесты
```bash
# Автоматический запуск (рекомендуется)
npm run test:e2e

# Ручной запуск
# 1. Запустите сервер в одном терминале
npm run dev

# 2. Запустите тесты в другом терминале
npm test -- --testPathPattern=e2e
```

## 📊 Анализ производительности

### Счетчики ререндеров
В демонстрации каждый компонент показывает количество ререндеров.
Используйте это для:

1. **Оптимизации**: Убедитесь, что компоненты не ререндерятся без необходимости
2. **Отладка**: Найдите компоненты с избыточными ререндерами
3. **Валидация**: Проверьте правильность графа зависимостей

### Пример анализа
```
При изменении First Name:
- firstName: 2 ререндера (изменение + обновление)
- fullName: 2 ререндера (зависит от firstName)
- profileSummary: 2 ререндера (зависит от fullName)
- lastName: 1 ререндер (не изменился)
- age: 1 ререндер (не изменился)
```

## 🔍 Инструменты разработчика

### React DevTools
1. Установите расширение React Developer Tools
2. Откройте вкладку Components
3. Проверьте пропсы и состояние компонентов
4. Используйте Profiler для анализа производительности

### Redux DevTools
1. Установите расширение Redux DevTools
2. Откройте вкладку Redux
3. Наблюдайте за изменениями состояния
4. Используйте Time Travel для отладки

### Консоль браузера
1. Откройте консоль (F12)
2. Проверьте наличие ошибок
3. Используйте console.log для отладки
4. Проверьте сетевые запросы

## 🚀 Производительность

### Оптимизации
1. **Селективное обновление**: Компоненты обновляются только при изменении их атомов
2. **Мемоизация**: Вычисляемые атомы кэшируют результаты
3. **Batch операции**: Группировка изменений для минимизации ререндеров

### Мониторинг
1. **React Profiler**: Анализ времени рендеринга
2. **Performance tab**: Анализ производительности браузера
3. **Счетчики ререндеров**: Встроенные в демонстрацию

## 🐞 Распространенные ошибки

### Ошибка: "atom2.read is not a function"
**Причина:** Неправильная инициализация атомов
**Решение:** Убедитесь, что атомы созданы с помощью функции `atom()`

### Ошибка: "Cannot read property" в вычисляемых атомах
**Причина:** Обращение к несуществующему атому
**Решение:** Проверьте зависимости вычисляемых атомов

### Ошибка: DevTools не подключаются
**Причина:** Расширение не установлено или отключено
**Решение:** Установите Redux DevTools и включите в настройках приложения

## 📈 Метрики

### Ключевые метрики
1. **Время первого рендера**: < 100мс
2. **Время обновления**: < 16мс (60 FPS)
3. **Количество ререндеров**: Минимально необходимое
4. **Размер бандла**: < 100KB (gzipped)

### Измерение
```javascript
// Пример измерения времени
console.time('update');
store.set(someAtom, newValue);
console.timeEnd('update');
```

## 🔄 Обновление зависимостей

### Nexus State
```bash
# Проверка текущей версии
npm list @nexus-state/core

# Обновление
npm update @nexus-state/core @nexus-state/react @nexus-state/devtools
```

### Dev Dependencies
```bash
# Обновление всех dev зависимостей
npm update --save-dev

# Обновление конкретной зависимости
npm update jest --save-dev
```

## 🤝 Помощь и поддержка

### Документация
- [Nexus State Documentation](https://github.com/nexus-state/nexus-state)
- [React Documentation](https://reactjs.org/docs)
- [Jest Documentation](https://jestjs.io/docs)
- [Puppeteer Documentation](https://pptr.dev)

### Сообщество
- GitHub Issues для багов и feature requests
- Discord/Slack каналы
- Stack Overflow с тегом nexus-state

### Отчет о проблемах
При отчете о проблеме укажите:
1. Версию Node.js и npm
2. Версию Nexus State
3. Шаги для воспроизведения
4. Ожидаемое и фактическое поведение
5. Скриншоты или логи ошибок
```