# Запуск тестов

Для запуска тестов в проекте необходимо сначала установить зависимости:

```bash
pnpm install
```

После установки зависимостей можно запускать тесты:

```bash
pnpm test
```

Если возникает ошибка "sh: vitest: command not found", убедитесь, что зависимости установлены корректно.