# Nexus State React Demo

Демонстрационное приложение для библиотеки управления состоянием Nexus State.

## 🎯 Возможности демонстрации

### 1. Вычисляемые атомы (Computed Atoms)
- Автоматический пересчет при изменении зависимостей
- Вложенные вычисляемые атомы
- Эффективное кэширование результатов

### 2. Селективное обновление (Selective Updates)
- Компоненты обновляются только при изменении их атомов
- Минимизация ререндеров
- Оптимизация производительности

### 3. DevTools интеграция
- Отслеживание изменений состояния
- Time travel debugging
- Группировка batch операций

### 4. Валидация состояния
- Автоматическая проверка валидности формы
- Вычисляемые атомы для валидации
- Визуальная индикация состояния

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: [http://localhost:5173](http://localhost:5173)

## 🧪 Тестирование

### Unit тесты
```bash
# Запуск всех unit тестов
npm run test:unit

# Запуск в watch режиме
npm run test:watch

# Покрытие кода
npm run test:coverage
```

### E2E тесты
```bash
# Запуск всех E2E тестов (автоматически запускает сервер)
npm run test:e2e

# Запуск всех тестов (unit + e2e)
npm run test:all
```

## 📁 Структура проекта

```
apps/demo-react/
├── src/
│   ├── App.jsx                 # Главный компонент приложения
│   ├── main.jsx                # Точка входа
│   ├── computed-atoms-demo/    # Демо вычисляемых атомов
│   │   └── index.jsx          # Основная демонстрация
│   └── minimal-test.jsx       # Минимальный тест
├── tests/                      # Unit тесты
│   ├── computed-atoms.test.js # Тесты вычисляемых атомов
│   └── react-integration.test.jsx # Тесты React интеграции
├── e2e/                       # E2E тесты
│   ├── basic-e2e.test.js      # Базовые E2E тесты
│   ├── simple-e2e.test.js     # Простые E2E тесты
│   └── computed-atoms-e2e.test.js # Комплексные E2E тесты
├── scripts/                   # Вспомогательные скрипты
│   └── run-e2e-tests.js      # Скрипт запуска E2E тестов
└── конфигурационные файлы
```

## 🎮 Использование демонстрации

### Демо 1: Простой счетчик
- Базовые атомы
- Вычисляемые атомы (удвоенное значение, проверка четности)
- DevTools интеграция

### Демо 2: Вычисляемые атомы и форма
- Форма с полями ввода
- Вычисляемые значения (полное имя, возраст, статус)
- Валидация формы
- Счетчики ререндеров для каждого атома
- Batch обновления

## 🔧 DevTools

### Установка Redux DevTools
1. Установите расширение Redux DevTools для вашего браузера
2. Откройте панель разработчика (F12)
3. Перейдите на вкладку Redux

### Возможности DevTools
- **Просмотр состояния**: Все атомы с их значениями
- **Time travel**: Навигация по истории изменений
- **Action лог**: Все изменения состояния с метаданными
- **Batch операции**: Группировка нескольких изменений

## 🧪 Примеры тестов

### Unit тесты
- Проверка вычисляемых атомов
- Проверка селективного обновления
- Проверка валидации
- Проверка batch операций

### E2E тесты
- Загрузка страницы
- Взаимодействие с формой
- Проверка вычисляемых значений
- Проверка селективного обновления
- Проверка валидации
- Проверка DevTools интеграции

## 🐛 Отладка

### Консольные сообщения
Приложение выводит в консоль:
- Сообщения о включении/выключении DevTools
- Предупреждения об ошибках
- Информацию об обновлениях атомов

### Проверка ререндеров
В демо вычисляемых атомов каждый компонент показывает счетчик ререндеров.
Это помогает убедиться, что компоненты обновляются только при необходимости.

### Проверка DevTools
1. Убедитесь, что расширение Redux DevTools установлено
2. Откройте панель разработчика
3. Перейдите на вкладку Redux
4. Выполните действия в приложении
5. Наблюдайте за изменениями в DevTools

## 📊 Метрики производительности

### Счетчики ререндеров
Каждый атом в демонстрации показывает количество ререндеров.
Это позволяет:
1. Убедиться в селективном обновлении
2. Оптимизировать компоненты
3. Понимать граф зависимостей

### Пример оптимизации
При изменении только поля "First Name":
- Обновляется только `firstName` атом
- Обновляется `fullName` (зависит от firstName)
- Обновляется `profileSummary` (зависит от fullName)
- **Не обновляются**: lastName, age, isActive и их производные

## 🔄 Batch операции

### Что такое batch операции?
Batch операции позволяют группировать несколько изменений состояния в одну транзакцию.

### Преимущества:
1. **Одна запись в DevTools** вместо нескольких
2. **Синхронное обновление** всех зависимостей
3. **Оптимизация производительности**

### Пример использования:
```javascript
// Вместо трех отдельных обновлений
store.set(firstNameAtom, 'Jane');
store.set(lastNameAtom, 'Smith');
store.set(ageAtom, 25);

// Одно batch обновление
store.startBatch('update-profile');
store.set(firstNameAtom, 'Jane');
store.set(lastNameAtom, 'Smith');
store.set(ageAtom, 25);
store.endBatch('update-profile');
```

## 🚨 Распространенные проблемы

### DevTools не отображаются
1. Проверьте, установлено ли расширение Redux DevTools
2. Убедитесь, что DevTools включены в настройках приложения
3. Проверьте консоль на наличие ошибок

### E2E тесты не запускаются
1. Убедитесь, что порт 5173 свободен
2. Проверьте установку puppeteer: `npm list puppeteer`
3. Запустите сервер вручную: `npm run dev`
4. Запустите тесты: `npm test -- --testPathPattern=e2e`

### Вычисляемые атомы не обновляются
1. Проверьте зависимости атомов
2. Убедитесь, что базовые атомы действительно изменяются
3. Проверьте консоль на наличие ошибок

## 📈 Дальнейшее развитие

### Планируемые улучшения
1. **Персистентность**: Сохранение состояния в localStorage
2. **Оптимизации**: Мемоизация сложных вычислений
3. **Инструменты**: DevTools плагин для Nexus State
4. **Тестирование**: Больше E2E сценариев

### Вклад в проект
1. Клонируйте репозиторий
2. Установите зависимости
3. Запустите тесты
4. Создайте pull request

## 📄 Лицензия

MIT