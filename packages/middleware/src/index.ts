/**
 * @deprecated This package is deprecated. Use `@nexus-state/extras/middleware` instead.
 * This re-export will be removed in v1.1.0.
 *
 * @example
 * // Old (deprecated)
 * import { middleware } from '@nexus-state/middleware';
 *
 * // New (recommended)
 * import { middleware } from '@nexus-state/extras/middleware';
 */
export * from '@nexus-state/extras/middleware';
