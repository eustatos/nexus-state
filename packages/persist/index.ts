/**
 * @deprecated This package is deprecated. Use `@nexus-state/extras/persist` instead.
 * This re-export will be removed in v1.1.0.
 *
 * @example
 * // Old (deprecated)
 * import { persist } from '@nexus-state/persist';
 *
 * // New (recommended)
 * import { persist } from '@nexus-state/extras/persist';
 */
export * from '@nexus-state/extras/persist';
